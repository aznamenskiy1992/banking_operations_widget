from typing import Union


def filter_by_currency(transactions: list[dict[str, int]], currency: str) -> Union[list[dict[str, int]], list[None]]:
    """Функция, фильтрующая список словарей с транзакциями по указанной валюте"""
    pass
